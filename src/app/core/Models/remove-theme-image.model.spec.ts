import { RemoveThemeImage } from './remove-theme-image.model';

describe('RemoveThemeImage', () => {
  it('should create an instance', () => {
    expect(new RemoveThemeImage()).toBeTruthy();
  });
});
