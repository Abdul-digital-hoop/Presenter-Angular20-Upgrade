import { PresentationPhase } from './presentation-phase.model';

describe('PresentationPhase', () => {
  it('should create an instance', () => {
    expect(new PresentationPhase()).toBeTruthy();
  });
});
