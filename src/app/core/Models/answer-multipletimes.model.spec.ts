import { AnswerMultipletimes } from './answer-multipletimes.model';

describe('AnswerMultipletimes', () => {
  it('should create an instance', () => {
    expect(new AnswerMultipletimes()).toBeTruthy();
  });
});
