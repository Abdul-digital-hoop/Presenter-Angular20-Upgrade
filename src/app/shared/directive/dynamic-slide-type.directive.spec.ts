import { DynamicSlideTypeDirective } from './dynamic-slide-type.directive';

describe('DynamicSlideTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicSlideTypeDirective();
    expect(directive).toBeTruthy();
  });
});
