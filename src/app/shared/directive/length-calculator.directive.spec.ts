import { LengthCalculatorDirective } from './length-calculator.directive';

describe('LengthCalculatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LengthCalculatorDirective();
    expect(directive).toBeTruthy();
  });
});
