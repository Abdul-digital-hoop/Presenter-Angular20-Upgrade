import { SlideThemesPipe } from './slide-themes.pipe';

describe('SlideThemesPipe', () => {
  it('create an instance', () => {
    const pipe = new SlideThemesPipe();
    expect(pipe).toBeTruthy();
  });
});
