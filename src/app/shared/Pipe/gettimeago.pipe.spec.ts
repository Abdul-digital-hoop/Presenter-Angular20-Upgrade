import { GettimeagoPipe } from './gettimeago.pipe';

describe('GettimeagoPipe', () => {
  it('create an instance', () => {
    const pipe = new GettimeagoPipe();
    expect(pipe).toBeTruthy();
  });
});
