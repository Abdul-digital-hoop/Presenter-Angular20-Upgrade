import { QuestionVisibilityPipe } from './question-visibility.pipe';

describe('QuestionVisibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionVisibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
