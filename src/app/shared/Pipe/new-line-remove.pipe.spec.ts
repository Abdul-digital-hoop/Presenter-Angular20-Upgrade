import { NewLineRemovePipe } from './new-line-remove.pipe';

describe('NewLineRemovePipe', () => {
  it('create an instance', () => {
    const pipe = new NewLineRemovePipe();
    expect(pipe).toBeTruthy();
  });
});
