import { SpaceBetweenNumbersPipe } from './space-between-numbers.pipe';

describe('SpaceBetweenNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceBetweenNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
